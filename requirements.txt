# Core
torch>=2.4            # install the CUDA build matching your GPU first
transformers>=4.44
datasets>=2.20
huggingface_hub>=0.24

# Interpretability / inspection
nnsight>=0.7          # used for interactive activation inspection (see docs/NNSIGHT_NOTES.md)
transformer-lens>=2.0 # optional cross-check
einops>=0.8

# Numerics / plotting / notebook
numpy>=1.26
matplotlib>=3.8
plotly>=5.20
jupyterlab>=4.0
ipykernel>=6.29
